A temporary text file with developer notes or reminders.========================================================SPARQL Query for resources. The 'optional' part needs updating after Mulgara gets updated.PREFIX dc: <http://purl.org/dc/elements/1.1/>PREFIX fedora-rels-ext: <info:fedora/fedora-system:def/relations-external#>PREFIX fedora-model: <info:fedora/fedora-system:def/model#>PREFIX fedora-view: <info:fedora/fedora-system:def/view#>SELECT $object $title $model $parent_model $created $identifier $mods $tnFROM <#ri>WHERE {{  $object fedora-model:label $title ;          fedora-model:hasModel $model ;          fedora-model:state fedora-model:Active ;          fedora-model:createdDate $created ;          fedora-rels-ext:isMemberOfCollection <info:fedora/si:292> .  $model fedora-model:hasModel $parent_model .  FILTER (sameTerm($parent_model, <info:fedora/islandora:resourceCModel>))}UNION{  $object fedora-model:label $title ;          fedora-model:hasModel $model ;          fedora-model:state fedora-model:Active ;          fedora-model:createdDate $created ;          fedora-rels-ext:isMemberOf <info:fedora/si:292> .  $model fedora-model:hasModel $parent_model .  FILTER (sameTerm($parent_model, <info:fedora/islandora:resourceCModel>))  }OPTIONAL{#  $object dc:identifier $identifier .#  CONCAT("<info:fedora/", "si:596/MODS>") fedora-view:disseminationType $mods .#  <info:fedora/si:596/TN> fedora-view:disseminationType $tn  $object dc:identifier $identifier .  LET ($new := IRI(fn:concat(str($object), "/MODS")))  $new fedora-view:disseminationType $mods .  $new fedora-view:disseminationType $tn .}}ORDER BY $titleLIMIT 25OFFSET 0/*So Concepts are collections that have a content model X. That content model X has the islandora:collectionCModel as a model.so concept -> hasModel X , X -> hasModel islandora:collectionCModelA resource initally was anything else but will soon be formalizedto beresource -> hasModel X, X -> hasModel islandora:resourceCModelNow to get all resources you would need something like this.select $object $title $model $parent_model $created from <#ri>where($object <info:fedora/fedora-system:def/model#label> $titleand $object <fedora-model:hasModel> $modeland $model <fedora-model:hasModel> $parent_modeland $parent_model <mulgara:is> <info:fedora/islandora:resourceCModel>and $object <fedora-model:createdDate> $createdand $object <fedora-model:state> <info:fedora/fedora-system:def/model#Active>and ($object <fedora-rels-ext:isMemberOfCollection> <info:fedora/COLLECTIONPID>or $object <fedora-rels-ext:isMemberOf> <info:fedora/COLLECTIONPID>))order by $titlewhere COLLECTIONPID is the parent Concept.module_load_include('inc', 'fedora_repository', 'CollectionClass');    $collection = new CollectionClass();    $results = $collection->getRelatedObjects($this->pid, 1000000, NULL, $query);Where the above query would go into $query and the pid is the COLLECTIONPIDIt will return some XML that can be processed to get the values you require*/